@page
@model TodoList.Pages.Notes.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Nouvelle tâche</h1>

<h4>Note</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Note.Title" class="control-label"></label>
                <input asp-for="Note.Title" class="form-control" />
                <span asp-validation-for="Note.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Note.Contenu" class="control-label"></label>
                <input asp-for="Note.Contenu" class="form-control" />
                <span asp-validation-for="Note.Contenu" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <input asp-for="Note.Priorite" class="form-check-input" />
                <label asp-for="Note.Priorite" class="form-check-label">Priorité</label>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
